#include <iostream>
#include"Stack.h"
using namespace std;

int main()
{
    /*
     // Another way to test Stack :
	Stack sst;
	sst.push(2);
	sst.push(4);
	sst.push(6);
	 
	while (sst.empty() == false)
	{
		cout << sst.top() << endl;
		sst.pop();
	}
	*/
	
	/*
	Stack st;
	st.push(10);
	st.push(15);
	st.push(20);
	st.push(25);
	cout << st.top() << endl;
	st.pop();
	cout << st.top() << endl;
	st.pop();
	cout << st.top() << endl;
	st.pop();
	cout << st.top() << endl;
	st.pop();

	if (st.empty() == true)
	{
		cout << "5lawees" << endl;
	}
	else
	{
		st.pop();
		cout << st.top();
	}
	*/
	
	return 0;
}
